{
  "name": "datahog",
  "version": "1.0.0",
  "description": "Datahog",
  "scripts": {
    "installAll": "npm i && cd src && cd mock_api && npm i && cd ../app && npm i && cd ../..",
    "testAll": "cd src && cd app && npm test && cd../..",
    "startMockApi": "cd src/mock_api && npm run start && cd../..",
    "startApp": "cd src/app && npm run start && cd../.."
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "supertest": "^6.1.3"
  }
}
