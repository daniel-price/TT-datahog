## Prerequisites

`pip install awscli-local localstack`

Ensure docker is running

## Install

`npm run installAll`

## Test

`npm run testAll`

## Run

`docker-compose up`

`npm run startMockApi`

`npm run startApp`
